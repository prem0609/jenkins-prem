pipeline {
    agent {
   label {
   label'built-in'
   customWorkspace "/mnt/mulitibranchpipeline-1"
   }
   }
   stages {
        stage('Hello') {
            steps {
                echo 'Hello everyone 22q2 file'
            }
        }
	  stage ('one') {
					steps {
							 sh "echo 'Hello priyanka' > qa.html"
					}
			}
}
}
