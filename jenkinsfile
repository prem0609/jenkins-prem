pipeline {
    agent {
   label {
   label'built-in'
   customWorkspace "/mnt/mulitibranchpipeline-1"
   }
   }
   stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
	  stage ('one') {
					steps {
							 sh "echo 'Hello PREM' > dev.html"
					}
			}
}
